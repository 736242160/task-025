# task-025: Python 单文件内存 SQL 查询引擎

零外部依赖：SELECT 列/*、WHERE 多条件 AND/OR/括号优先级、ORDER BY 升降序、GROUP BY + COUNT/SUM/AVG 聚合、LIMIT；字符串/整数/小数类型严格匹配比较；词法语法错误定位到字符位置，区分列名不存在/语法错/类型不匹配；附多条件/排序/聚合/LIMIT/类型错误/语法错误测试用例。
